services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: pharmindo-backend
    restart: unless-stopped
    env_file:
      - ./apps/backend/.env
    ports:
      - "127.0.0.1:4000:4000"
